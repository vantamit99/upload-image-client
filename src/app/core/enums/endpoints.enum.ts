export enum USER_ENDPOINT {
    USER = "users"
}
export enum PROFILE_ENDPOINT {
    PROFILE = "profiles"
}
export enum LANGUAGE_ENDPOINT {
    LANGUAGE = "language"
}
export enum UPLOAD_ENDPOINT {
    UPLOAD = "upload"
}